# ARIDO BACKEND CHALLENGE

```
    "author": "Dario Frison",
    "description": "step 2 of Arido Challenge",
    version: "1.0.0",
    tools: "Node.js, Express, mysql2, Sequelize, bcryptjs, API Rest "
    "scripts": {
        "start": "cd src && nodemon app.js"
    }

    Importante: Tener en cuenta que se subio el archivo config.js, el cual
    se encuentra en src/database/config/config.js y que contiene la correspondiente
    configuracion de la app con la base de datos, se debera ingresar en el mismo
    su propia user y password. Si bien no archivos que deben ser ignorados, el mismo 
    se cargo a gitHub por fines practicos.
```